# Specs Relocated

Feature specifications now live under ``docs/4-architecture/features`/<NNN>/spec.md`.

This directory is kept as a breadcrumb for older references; update links to the new per-feature paths.
